name: pynteny-dev
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - python >= 3.8
  - poetry >= 1.3
  - pip
  - pyfastx >=0.8
  - biopython >= 1.8
  - numpy
  - pandas
  - psutil >= 5.9
  - requests
  - tqdm
  - pip:
    - pyhmmer >=0.10
    - pyrodigal >=3.0
    - mkdocs
    - mkdocs-gen-files
    - pymdown-extensions
    - mkdocs-jupyter
    - mkdocstrings[python]
    - ruff
    - black
    - argmark
    - coverage
