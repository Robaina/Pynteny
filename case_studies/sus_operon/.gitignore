# Regenerated by run_case_study.sh / the notebook (downloaded / built), not version-controlled
data/genomes/
data/peptides/

# Raw per-HMM HMMER dumps and logs (noisy); the curated synteny_matched.tsv,
# per-gene hit FASTAs and summary.tsv ARE kept.
results/**/hmmer_outputs/
results/**/pynteny.log
